import * as flsFunctions from "./modules/functions";
flsFunctions.isWebp();

let str: string;
str = "asda";
console.log("str: ", str);

const el = document.querySelector(".container")!;
console.log("el: ", el);
